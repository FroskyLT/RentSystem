import React from 'react';
import styles from './header.module.scss';

export const Header = () => {
  return (
    <header className={styles.header}>
      {"Justino ir Davido Rent System"}
    </header>
  );
}

export default Header;
